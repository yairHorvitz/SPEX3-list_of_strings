#include <stdio.h>
#include <stdlib.h>
#define MAX 100
int main(int argc, char const *argv[])
{
    int x;
    scanf("%d", &x);
    char str [MAX];
    scanf("%c", &str);

    for (size_t i = 0; i < x; i++)
    {
        /* code */
    }
    ;
}
